<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.co.spookypeanut.lovetag" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Love"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:parentActivityName=".Love" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="uk.co.spookypeanut.lovetag.Love" />
        </activity>
        <activity
            android:name=".TagInput"
            android:label="@string/title_activity_tag_input"
            android:parentActivityName=".Love" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="uk.co.spookypeanut.lovetag.Love" />
        </activity>

        <receiver android:name=".LoveWidget" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/love_widget_info" />
        </receiver>
        <service android:name=".LoveWidget$UpdateService" />
    </application>

</manifest>
